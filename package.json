{
  "name": "magic-shuffle",
  "version": "1.0.0",
  "description": "Magic Shuffle — personalized song picker from biometrics, weather, and calendar context; Spotify + optional playback.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
