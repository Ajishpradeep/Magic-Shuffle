{
  "name": "moodagent",
  "version": "1.0.0",
  "description": "Map biometric signals (energy, stress, sleep) to Spotify music recommendations and play them in-browser.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
